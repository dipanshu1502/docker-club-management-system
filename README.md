# Club Management System (Docker)

## Steps to Run

1. Clone the repository
git clone <your-link>

2. Go to folder
cd club-management

3. Run Docker
docker-compose up --build

4. Open browser
http://localhost:8080